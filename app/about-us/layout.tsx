import type { ReactNode } from "react"

export const metadata = {
  title: "About Us - Medical Center",
  description: "LMS for medical professionals",
}

export default function AboutLayout({ children }: { children: ReactNode }) {
  return children
}
