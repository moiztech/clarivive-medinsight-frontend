import type { ReactNode } from "react"

export const metadata = {
  title: "Courses - Clarivive Medinsight",
  description: "LMS for medical professionals",
}

export default function CoursesLayout({ children }: { children: ReactNode }) {
  return children
}
